worker:
  replicaCount: 1

global:
  conf:
    app:
      django_settings_module: config.settings
      superuser:
        username: "admin"
        email: "admin@example.com"
      media_dir: "/app/media"
      logging_config: ''
      tz: GMT
    ingress:
      enabled: true
      host: papermerge.minikube
    db:
      type: "postgres"
      user: "postgres"
      name: "postgres"
    es:
      enabled: false
  secrets:
    db:
      password: ''
    app:
      secret_key: ''
      superuser:
        password: ''